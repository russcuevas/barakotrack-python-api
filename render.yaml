services:
  - type: web
    name: barakotrack-python-api
    env: python
    rootDir: python_service
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
